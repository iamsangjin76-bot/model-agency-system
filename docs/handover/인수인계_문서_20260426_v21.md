# 인수인계 문서 v21 (2026-04-26)

> **이전 버전**: v20 (2026-04-25)
> **드라이브**: G:\ (v19부터 F→G 이전 완료)
> **작업 경로**: `G:\Project M\model-agency-system`
> **handover 폴더**: `G:\Project M\model-agency-system\handover\`

---

## 0. 새 세션 시작 시 첫 작업

새 Claude Code 세션 시작 시 다음 명령으로 권장 모델 확인:

```
claude --model sonnet
```

(J-8b 마무리는 단순 시각 검증 + commit 작업이라 Sonnet 4.6 적합)

이 인수인계 문서를 새 claude.ai 창에 첨부하면 즉시 컨텍스트 복원 가능.

---

## 1. 현재 상태 한눈에 (2026-04-26 기준)

### ✅ 완료된 마일스톤

| Phase | 내용 | commit |
|---|---|---|
| J-1 ~ J-5 | 모델 관리, 캐스팅, 정산 | (이전 commits) |
| J-6 | 통합 검증 + graceful 503 fix | `99253666` |
| /auto setup | `.autopus/project/` 6개 컨텍스트 문서 자동 생성 | (commit 미진행, 디스크 적용) |
| **/auto setup 정정** | 라우터/페이지/DB 모델 카운트 모순 3건 정정 | (commit 미진행) |
| **J-8a 백엔드** | 외부 이미지 프록시 (SSRF 방어 6항목 + 디스크 캐시) | `c458bed5` ✅ |
| **SPEC-IMAGE-PROXY-002 작성** | spec-writer 자동 생성 (J-8b 프론트엔드 통합) | (디스크 적용) |
| **SPEC-002 디렉토리 정정** | 단일 .md 파일 → 표준 4파일 디렉토리 구조 | (디스크 적용) |
| **J-8b 코드 변경** | imageProxy.ts 신규 + 3 컴포넌트 4 call site 수정 | (commit 미진행) |
| **J-8b Key 정정** | snake_case 통일 (백엔드 ↔ 프론트 일치) | (commit 미진행) |
| **Vite 환경 리셋** | PID 40656 → 48660, .vite 캐시 삭제, 1897ms 새 빌드 | (커밋 대상 아님) |

### 🔴 진행 중 — 다음 작업

**J-8b 마지막 시각 검증**:
- Jin이 브라우저에서 Ctrl+Shift+R 후 이미지 검색
- 결과: 미보고 (새 세션 첫 단계)
- 통과 시 → Lore commit → /auto sync

### 🔴 미착수

- J-7 라이브러리 관리 뷰
- J-8c ImageSearchPage UX 패리티 (NewsSearch 자동 매칭 패턴 복제)
- J-8d DashboardPage.tsx:278 PlaceholderPage 다크 짝 1줄 보정
- Phase K-1 프로필 내보내기 (4 템플릿)
- SPEC-SNS-001 (Instagram/TikTok/YouTube)
- Phase I-1 Electron 패키징
- SPEC-IMAGE-PROXY-001 단일 파일 → 표준 디렉토리 구조 정규화 (선택)
- J-8a 후속 보안 fix: R1 (`fe80::/10` IPv6 link-local), R2 (CSP 헤더)

---

## 2. 핵심 사실 — 새 Claude가 알아야 할 것

### 2.1 J-8a (commit `c458bed5`) — 백엔드 이미지 프록시 완료

**구현 파일** (9개, +1336/-2):
- 신규: `backend/app/routers/proxy.py`, `backend/app/services/image_proxy_service.py`, `backend/tests/test_image_proxy_mismatch.py`
- 변경: `backend/app/utils/security.py` (validate_proxy_host 추가), `backend/app/config.py` (5 env), `backend/app/main.py` (router 등록), `.gitignore` (proxy_cache/)
- 신규: `.autopus/specs/SPEC-IMAGE-PROXY-001.md` (단일 파일 — 비표준 패턴), `.autopus/plans/PLAN-J-8a.md`

**SSRF 방어 6항목 모두 PASS**: 도메인 화이트리스트 / private IP CIDR / 10MB 제한 / 5s timeout / Content-Type+magic 이중 검증 / 디스크 캐시 7일 TTL.

**검증 결과**: reviewer TRUST 5 + S1~S11 + N1~N4 = 20/20 PASS, security-auditor OWASP A10 6/6 PASS, Critical 0 / Medium 0 / Low 2 (R1 fe80::/10, R2 CSP — 후속 fix).

**.env**: `IMAGE_PROXY_ALLOWED_DOMAINS`, `IMAGE_PROXY_MAX_SIZE=10485760`, `IMAGE_PROXY_TIMEOUT=5.0`, `IMAGE_PROXY_CACHE_DIR=proxy_cache`, `IMAGE_PROXY_CACHE_TTL=604800` 5개 설정 완료.

### 2.2 J-8b — 프론트엔드 이미지 프록시 통합 (커밋 대기 중)

**구현 파일** (4개):
- 신규: `frontend/src/utils/imageProxy.ts` (55줄) — `proxify(url)` + `IMAGE_PROXY_PLACEHOLDER` SVG + `handleImgError`
- 변경: `frontend/src/components/search/ImageResultCard.tsx` (line 23 — proxify + onError)
- 변경: `frontend/src/components/search/ImagePreviewModal.tsx` (line 28 img src + line 50 a href)
- 변경: `frontend/src/components/model-detail/ModelImageGallery.tsx` (line 73 + line 136, 4 call site 중 2개)

**Key 정정 추가 변경** (Jin 화면에서 placeholder 발견 후 진단):
- 변경: `frontend/src/services/domain-api.ts` line 230 SearchImage interface — `thumbnailUrl/originalUrl` → `thumbnail_url/original_url` (snake_case 통일)
- ImageResultCard, ImagePreviewModal 사용처도 snake_case로 변경

**npm run build**: PASS (exit 0, tsc 0 errors, vite 9.43s, 2216 modules transformed).

**SPEC**: `.autopus/specs/SPEC-IMAGE-PROXY-002/` (표준 4파일 디렉토리 구조)
- spec.md (174줄), plan.md (68줄), acceptance.md (24줄), research.md (57줄)

### 2.3 J-8b 메타 결함 — 진단 과정에서 발견한 핵심 사실

**이전 진단 오류**: J-6 부수 이슈 #1 "외부 이미지 핫링크 차단"이 잘못 진단됐음. 진짜 원인은 **백엔드 snake_case ↔ 프론트엔드 camelCase Key 불일치**로 인한 `image.thumbnailUrl === undefined`. J-8a 백엔드 프록시는 결과적으로 정상 작동하지만 이 문제와 무관했음.

**의미**: J-8a 작업 자체가 불필요했던 게 아니라, 본 사용자가 보고하는 증상의 원인 진단이 잘못됐었음. 백엔드 프록시는 그 자체로 가치 있음 (향후 핫링크 차단 도메인 대응).

### 2.4 Vite 환경 리셋 (2026-04-26 18:00경)

J-8b 코드 변경 후 Jin 브라우저에서 변경 미반영 상태가 지속돼 다음 작업 수행:
- PID 40656 (옛 Vite) Stop-Process -Force
- `node_modules/.vite` 캐시 폴더 삭제
- npm run dev 재실행 → PID 48660, 부팅 1897ms (이전 342ms 대비 5배 = 캐시 miss = 새로 빌드)
- 5173 LISTEN 확인

→ Vite memory + cache 모두 깨끗이 정리. 코드 적용 보장.

### 2.5 진단 과정에서 발견한 추가 사실

- `imgnews.naver.net`은 SPEC-IMAGE-PROXY-001 화이트리스트에 등록됨
- 검색 결과 thumbnail URL은 모두 `https://search.pstatic.net/...` 형식 (Naver의 자체 thumbnail proxy)
- 외부 도메인 차단 위험 도메인: `i1.sndcdn.com`, `i.pinimg.com`, `cdn2.ppomppu.co.kr` 등은 화이트리스트에 없음 — 향후 J-8c/추가 작업에서 검토 필요

---

## 3. 새 세션 첫 단계 — 명확한 행동 지침

### 3.1 Jin이 답해야 할 것 (새 창에서 첫 메시지)

새 claude.ai 창에 본 인수인계 문서를 첨부하고 다음과 같이 알려주세요:

```
v21 인수인계 첨부. J-8b Vite 환경 리셋(PID 48660) 완료한 직후 상태.
브라우저 강력 새로고침 후 이미지 검색 결과:
- ✅ "잘 됨" (이미지 정상 표시) → Lore commit 단계로
- ❌ "그대로" (placeholder 지속) → 추가 진단 필요
- ⚠️ "일부만" → 부분 성공 분석
```

### 3.2 결과별 다음 단계

**A. "잘 됨" 시**:
1. Lore commit 작성 (PLAN §9 skeleton 사용)
2. commit message:
```
feat(frontend,proxy): integrate /api/proxy/image in search components

J-8b: 프론트엔드 이미지 프록시 통합 (SPEC-IMAGE-PROXY-002)

변경 파일:
- frontend/src/utils/imageProxy.ts          신규 -- proxify() + handleImgError + IMAGE_PROXY_PLACEHOLDER
- frontend/src/services/domain-api.ts       변경 -- SearchImage interface snake_case 통일
- frontend/src/components/search/ImageResultCard.tsx   변경 -- 1 call site
- frontend/src/components/search/ImagePreviewModal.tsx 변경 -- 2 call site (img src + a href)
- frontend/src/components/model-detail/ModelImageGallery.tsx 변경 -- 2 call site
- .autopus/specs/SPEC-IMAGE-PROXY-002/      신규 -- 표준 디렉토리 (spec/plan/acceptance/research)

[메타 결함 정정] J-6 부수 이슈 #1 진단 오류 발견: 핫링크 차단이 아니라 백엔드 snake_case ↔
프론트엔드 camelCase Key 불일치. domain-api.ts SearchImage를 snake_case로 통일.

검증:
- npm run build PASS (tsc 0 errors, vite 9.43s)
- AC1~AC4, AC8, AC9 자동 검증 PASS
- AC5~AC7 Jin 수동 시각 검증 PASS

Constraint: 백엔드 코드 무변경, NewsSearchPage 무변경
Rejected: 변환 어댑터 추가 (대신 직접 key 변경)
Confidence: high
Scope-risk: module
Reversibility: trivial
Directive: J-8c (ImageSearchPage UX 패리티) 진입 시 ImageSearch ↔ NewsSearch 매칭 패턴 분리
Tested: npm build, Jin 브라우저 시각 검증 V1~V8
Not-tested: vitest unit test (인프라 부재 — Phase I-3)
Related: SPEC-IMAGE-PROXY-002, SPEC-IMAGE-PROXY-001 (parent)

🐙 Claude Code <noreply@anthropic.com>
```
3. `/auto sync SPEC-IMAGE-PROXY-002` 실행 → 문서 동기화

**B. "그대로" 시**:
1. 백엔드 로그에서 `/api/proxy/image` 호출 발생 여부 확인
2. 호출 발생 → 코드 적용 완료, fetch 단계 결함 (외부 사이트 차단 등)
3. 호출 0건 → 브라우저 자체 캐시 (시크릿 창 강제 시도)

**C. "일부만" 시**:
1. 정상/비정상 케이스 분리 (특정 도메인만 깨지는지)
2. 화이트리스트 미등록 도메인 가능성 → SPEC-IMAGE-PROXY-001 §6.2 확장 검토

---

## 4. 환경 정보 (필수)

### 4.1 작업 환경

- **OS**: Windows + PowerShell
- **드라이브**: G:\ (v19부터 F→G 이전, `cd "G:\Project M\model-agency-system"` 형식 사용 — 공백 경로 따옴표 필수)
- **백엔드**: FastAPI 0.109 + Python 3.12, uvicorn 포트 8000
- **프론트엔드**: React + TypeScript + Vite 5.4.21 포트 5173 (메모리 v20에 5174로 적혀 있으나 실제 5173)
- **Vite 현재 PID**: 48660 (2026-04-26 리셋)
- **Git**: 사용자 "Sangjin", 이메일 `iam.sangjin76@gmail.com`
- **GitHub**: `iamsangjin76-bot/model-agency-system` (private)

### 4.2 Vite proxy 설정 (vite.config.ts 검증됨)

```ts
server: {
  port: 5173,
  proxy: {
    '/api': { target: 'http://localhost:8000', changeOrigin: true },
    '/uploads': { target: 'http://localhost:8000', changeOrigin: true },
  },
}
```

### 4.3 .env (백엔드)

J-8a 5개 환경변수 활성:
- `IMAGE_PROXY_ALLOWED_DOMAINS` (suffix-only 화이트리스트)
- `IMAGE_PROXY_MAX_SIZE=10485760`
- `IMAGE_PROXY_TIMEOUT=5.0`
- `IMAGE_PROXY_CACHE_DIR=proxy_cache`
- `IMAGE_PROXY_CACHE_TTL=604800`

### 4.4 테스트 인프라 결함 (변동 없음)

- **vitest**: 미구성 (Phase I-3 의존)
- **Playwright**: 미구성 (Phase I-3)
- **pytest**: 시스템 Python 설치됐으나 `pydantic_settings` 부재 — Poetry 환경 전제

→ J-8b의 SPEC §6 SKIP 정책 준수 (TRUST 4, Tested N/A).

### 4.5 300줄 한도 Known Issues

- DashboardPage.tsx 445줄 (Known Issue #9, J-8d 별도 fix 대상)
- database.py 330줄 (Known Issue #8)
- 그 외 300줄 초과 파일 8개 (ARCHITECTURE.md §6 참조)

---

## 5. Autopus-ADK 동작 원리 — 새 Claude가 알아야 할 것

이 프로젝트는 Autopus-ADK v0.39.1 하네스 위에서 운영됩니다.

### 5.1 핵심 명령

| 명령 | 역할 |
|---|---|
| `/auto setup` | `.autopus/project/` 6개 컨텍스트 문서 자동 생성/갱신 |
| `/auto plan "..."` | spec-writer 호출 → SPEC 4문서 자동 작성 |
| `/auto go SPEC-ID` | executor 호출 → 실제 코드 구현 + Gate 검증 |
| `/auto sync SPEC-ID` | 문서 동기화 + Lore commit |

### 5.2 Quality 모드 — `autopus.yaml` balanced 기본

| 에이전트 | 모델 |
|---|---|
| spec-writer | **opus** |
| planner | **opus** |
| architect | **opus** |
| security-auditor | **opus** |
| executor | sonnet |
| reviewer | sonnet |
| validator | sonnet |
| tester | sonnet |

→ `/auto plan` 자동으로 Opus 사용. 별도 모델 지정 불요.

### 5.3 SPEC 표준 디렉토리 구조

```
.autopus/specs/SPEC-{DOMAIN}-{NUMBER}/
├── prd.md         (선택, --skip-prd로 생략 가능)
├── spec.md
├── plan.md
├── acceptance.md
└── research.md
```

**비표준 단일 파일** (예: `SPEC-IMAGE-PROXY-001.md`)은 `/auto go`/`/auto sync`가 SPEC_PATH 해석에 실패할 위험. SPEC-002는 정정 완료, SPEC-001은 후속 정규화 검토 (선택).

### 5.4 Lore commit 형식

```
type(scope): subject

본문

Constraint: ...
Rejected: ...
Confidence: low|medium|high
Scope-risk: local|module|system
Reversibility: trivial|moderate|difficult
Directive: ...
Tested: ...
Not-tested: ...
Related: SPEC-ID

🐙 ...
```

`Constraint` trailer 필수 (`autopus.yaml` 강제).

### 5.5 JARVIS 5항목 완료 형식

자가 보고 마지막에 5항목 한국어 명시 (Stop hook이 강제):
1. 적용한 HARNESS
2. 읽은 SKILL
3. 따른 G-STACK 단계
4. 검증 결과
5. 다음 단계

---

## 6. Jin 사용자 선호 사항 (변동 없음)

### 6.1 응답 정책 (절대 준수)

1. **항상 존댓말** (반말 금지)
2. **모델 선택 명시** — 작업 지시 전 Sonnet 4.6 / Opus 4.6 선택을 응답 중간 표가 아닌 에이전트 호출 직전 눈에 띄는 위치에
3. **한 단계만 제공** — 명령 포함 두 단계 연속 절대 금지
4. **Autopus-ADK 에이전트 호출 우선** — claude.ai 단독 설계 금지, planner → executor → reviewer 흐름
5. **새창 스냅샷** — 매 응답 자동 제공 금지, 토큰 한도 임박 또는 Phase 종료 마일스톤에서만
6. **솔직한 답변** — 추측 제한, 검증 후 답변

### 6.2 Jin이 명시적으로 거부한 패턴

- "그냥 넘어가라" 같은 게으른 답변 (정정 권고)
- 결정 떠넘기기 (A/B/C 선택지 제시 후 권고 없이 끝나는 것)
- 추측만 던지고 검증 안 하기
- 자신 있다고 말한 것이 사실 아닌 경우

### 6.3 Jin 작업 패턴

- **비개발자** — Network 탭 조작, Console 입력 등은 부담스러워하심
- **CLI에서 자동 실행 선호** — claude.ai에서 명령 작성하면 CLI에 붙여넣어 실행
- **시각 검증만** — 코드 변경 결과를 브라우저에서 확인하는 것
- **handover 폴더 직접 다운로드** — claude.ai에서 받는 모든 파일을 `G:\Project M\model-agency-system\handover\`에 직접 다운로드

---

## 7. 미해결 진단 가설 (J-8b 마지막 시각 검증 결과 따라 분기)

### 가설 A — 코드 정확, Vite 캐시 정리로 해결됨
- 가능성: 매우 높음 (90%)
- 검증: 1897ms 새 빌드 부팅 시간이 캐시 정리 증명
- 결과: 정상 표시 예상

### 가설 B — 브라우저 자체 캐시
- 가능성: 5%
- 검증: 시크릿 창 또는 Ctrl+Shift+Delete 캐시 삭제 후 재시도
- 결과: 시크릿 창에서 정상 표시되면 일반 브라우저 캐시 비우기로 해결

### 가설 C — 외부 사이트가 백엔드 IP/Referer까지 차단
- 가능성: 3%
- 검증: 백엔드 로그에서 `/api/proxy/image` 호출 시 502 (upstream 4xx) 발생 여부
- 해결: 백엔드 프록시 fetch 시 Referer 헤더 빈 값 또는 자체 도메인으로 설정

### 가설 D — 화이트리스트 미등록 도메인
- 가능성: 2%
- 검증: 검색 결과 도메인이 SPEC-IMAGE-PROXY-001 §6.2 화이트리스트에 있는지 대조
- 해결: 화이트리스트 확장 (별도 SPEC fix 작업)

---

## 8. 새 세션에서 우선 확인할 것

새 Claude가 이 인수인계를 읽고 첫 응답 전에 확인:

1. ✅ Vite 프로세스 살아있는지: `netstat -ano | findstr :5173`
2. ✅ 백엔드 살아있는지: `curl.exe -s http://localhost:8000/api/health` 또는 백엔드 PowerShell 창 확인
3. ✅ Git 상태: `cd "G:\Project M\model-agency-system" && git status` (J-8b 변경분 unstaged 상태여야 함)
4. ✅ Jin의 시각 검증 결과: 첫 메시지에서 "잘 됨" / "그대로" / "일부만" 확인

---

## 9. 진행 통계 (참고용)

- **현재 commit hash**: `c458bed5` (J-8a 백엔드)
- **다음 commit 대기**: J-8b 프론트엔드 통합 (4파일, +약 80줄)
- **SPEC 진행**: SPEC-AUTH-001 (✅) / SPEC-IMAGE-PROXY-001 (✅ 백엔드 종결) / SPEC-IMAGE-PROXY-002 (🟡 마지막 시각 검증 대기)
- **남은 J 작업**: J-8c, J-8d (소작업), J-7 (중작업)
- **다음 SPEC 영역**: K-1 (프로필 내보내기), SNS-001

---

## 10. 메모 — 새 Claude에게

이번 세션은 J-8a, /auto setup 정정, SPEC-IMAGE-PROXY-002 작성, 디렉토리 정정, J-8b 코드 적용, Key 불일치 진단/정정, Vite 환경 리셋까지 진행됐습니다. 작업량이 많아 토큰 사용량 누적이 큽니다.

**진단 과정의 메타 교훈** (Jin이 짜증을 내신 이유):
1. 처음에 추측만 던지고 검증 안 했음 (예: "도메인 화이트리스트 부족" 추측 → 사실 아님)
2. 정밀 분석 지시받고도 일부만 봐서 또 추측 단계로 빠짐
3. 결국 "정밀하게 분석" 두 번 지시받은 후에야 코드 + API 응답 + interface 4-source 교차 검증으로 진짜 원인(snake_case/camelCase 불일치) 발견

**새 Claude에게 권고**:
- Jin이 "확실해?", "정밀하게", "솔직하게" 같은 단어를 쓰시면 즉시 작업 멈추고 실측 데이터 기반 검증으로 전환
- 추측 가설은 1개만 제시하고 곧바로 검증 명령 실행
- 백엔드 응답과 프론트엔드 사용 형식의 일치 여부는 새 검색 기능 추가 시 항상 첫 번째 점검 항목
- 결정 회부할 때는 권고 1개를 명시 (선택지 나열만으로 끝내지 말 것)

🐙 v21 종료
