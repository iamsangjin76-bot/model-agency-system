# 🐙 Model Agency System — 작업 인수인계 문서 v8

작성일: 2026-04-16 (Phase D-1 B 완료 시점)
이전 문서: `인수인계_문서_20260416_v7.md`

---

## 🎯 이번 세션 한 줄 요약

> **"Phase D 진입 성공. B 항목(`.agent/browser/` 32개 파일) 감사·추적 해제·push 완결 → 커밋 `a72988b3`. C~G 5항목은 Phase D-2 (architect 통합 설계) 대상."**

---

## 📍 현재 git 상태 — 🟢 완전 동기화

### ✅ HEAD = origin/master 일치

```
a72988b3c5ba2ca79d4945d56499736f2ecf0e2b
```

**working tree: clean**. untracked 없음, modified 없음.

### 📜 Phase C + D 커밋 시리즈 (시간 역순, 총 9개)

| # | 커밋 해시 | 메시지 | 역할 | 세션 |
|---|---|---|---|---|
| 9 | `a72988b3` | `chore(gitignore): exclude .agent/browser/ snapshot artifacts from tracking` | **Phase D-1 B** | **이번 세션** |
| 8 | `fb170f8e` | `chore(gitignore): exclude local claude code settings from tracking` | 위생 정리 | 이전 |
| 7 | `ba5386c8` | `fix(templates): add missing A3 branding format canonical source` | 설계 산출물 | 이전 |
| 6 | `2236d5ca` | `docs(workflow): establish non-developer solo-play workflow` | 설계 산출물 | 이전 |
| 5 | `2eaf344d` | `chore(autopus): add auto plugin skills and codex plugin registration` | 설계 산출물 | 이전 |
| 4 | `156adcd0` | `docs(autopus): baseline project knowledge and harness manifests` | 설계 산출물 | 이전 |
| 3 | `224ca59a` | `chore(context): replace go-specific deny patterns with python/react rules` | 설정 | 이전 |
| 2 | `f57e06e6` | `fix(gitignore): allow .autopus/ tracking for project knowledge` | 설정 | 이전 |
| 1 | `a9e6258f` | `chore(config): switch commit language policy from en to ko` | 설정 | 이전 |

---

## ✅ 이번 세션에서 완료한 작업

### 1. Phase D-1 선행 정찰
- `git ls-files .agent/browser/` → 32개 파일 확인 (HTML 20 + PNG 12 + MD 4)
- 최초 커밋 추적 → `dc5c6575` (2026-04-15 initial commit 부터 노출)
- `.gitignore` 규칙 부재 확인 (근본 원인)
- 대형 PNG 2쌍 (~193KB) 식별

### 2. security-auditor 에이전트 텍스트 감사 (24개 파일)
- HTML 20 + MD 4 전수 스캔
- 패턴: API keys, JWT tokens, emails, IP, secrets, cookies, auth headers, localStorage, cloud provider keys
- **결과: 민감정보 0건 검출**
- 유일한 키워드 매치는 로그인 폼의 `type="password"` 속성뿐 (false positive)

### 3. Vision 이중 검증 (대형 PNG 4개)
- `screenshot_localhost_1771035061765.png` + `(2)` 복사본
- `screenshot_localhost_1771035139028.png` + `(2)` 복사본
- **결과: 전부 빈 로그인 폼. 입력값·토큰·세션·쿠키 없음**
- 앱명("Model Agency") + 푸터 문구 외 노출 없음

### 4. executor 에이전트 추적 해제 실행
- `.gitignore` 에 `# Browser agent capture artifacts (Phase D-1)` + `.agent/browser/` 블록 추가
- `git rm -r --cached .agent/browser/` → 인덱스만 제거, 로컬 32개 파일 보존
- Lore 커밋 `a72988b3` 생성 + push

### 5. push 경위 규명
- Contract 에 `git push 금지` 명시했으나 executor 가 push 실행함
- 사후 조사: `git reflog`, `git config`, `.claude/` 전수 → **자동 push 메커니즘 없음 확정**
- 원인: Contract 문구 "push 는 별도 단계" 를 executor 가 "완료 후 해도 됨" 으로 해석
- **해결책**: 다음 Contract 부터 `git push 일체 금지 — 이 Contract 범위에서는 단 한 번도 실행하지 않는다` 로 강화

---

## 🔴 지금 멈춘 지점

**Phase D-1 B 완전 종료. Phase D-2 (C~G 통합 설계) 진입 직전.**

사용자 판단으로 세션 종료 + v8 작성. Phase D-2 는 새 세션에서 시작.

---

## 🎓 이번 세션에서 얻은 신규 교훈

### 1. Lore pre-commit hook 의 enum 제약 🆕
- Hook 은 `Confidence: high` 같은 **bare enum 값만** 허용
- `Confidence: high (security-auditor + vision...)` 형식은 거부
- **아키텍트 교훈**: Worker Contract 작성 시 `Confidence` / `Scope-risk` / `Reversibility` 는 enum만, 부가 설명은 `Tested` / `Directive` / `Related` 필드로 분리
- 이번 세션 executor 가 자동 우회해서 커밋 성공했지만, 미리 알았으면 시간 절약 가능

### 2. Worker Contract [금지] 조항의 해석 여지 🆕
- "push 는 별도 단계" 같은 **조건부 문구**는 에이전트가 완화 해석할 여지 존재
- **아키텍트 교훈**: 금지는 항상 **절대형**으로 작성
  - ❌ `push 는 별도 단계에서`
  - ✅ `git push 일체 금지 — 이 Contract 범위에서는 단 한 번도 실행하지 않는다`
- Phase D-2 이후 Contract 부터 이 원칙 적용

### 3. Vision 검사는 5분 투자할 가치 있음 🆕
- security-auditor 가 `sensitiveNotFound` 결론 내고 "바로 D-2 가능" 제안
- 아키텍트가 "이미 push 된 상태 = 최악 가정" 원칙 적용해서 Vision 추가
- 결과는 동일(clean)했지만 **판단 과정의 완결성**이 중요 — 다른 항목에서 같은 주저 발생 시 기준점
- **원칙**: security 감사는 "충분한 증거" 가 아니라 "가능한 모든 경로 확인" 이 기본

### 4. 단일 항목은 architect 스킵 가능
- v7 체인: security-auditor → architect → planner → executor → reviewer
- 실제 적용: security-auditor → (architect 스킵) → executor
- **판단 근거**: 감사 결과가 분기 C (단순 추적 해제) 로 명확했고, `.gitignore + rm --cached` 패턴은 `fb170f8e` 에서 이미 검증됨
- **주의**: C~G 는 항목별로 처리가 다르므로 architect 필수. B 의 성공을 일반화하면 안 됨

### 5. 세션 길이 관찰 — 1 커밋 / 4 에이전트 호출이 쾌적 한계
- 이번 세션: 정찰(조회) + security-auditor + Vision + executor = 에이전트 4회 + 1 커밋 + push
- v6 기준 "3~4 커밋 = 세션 한계" → **에이전트 호출 수로도 같은 감각** 형성
- **아키텍트 교훈**: Phase D-2 는 architect + planner + executor + reviewer = 최소 4회 → 1 세션 = 1 커밋 그룹 예상

---

## 🗺️ Phase D-2 — C~G 통합 설계 가이드

### 📋 남은 오염 항목 5개

| 항목 | 대상 | 심각도 | 처리 예상 |
|---|---|---|---|
| **C** | `backend/app/config (2).py`, `main (2).py` 등 Windows `(2)` 복사본 | 🔴 높음 | 진짜 코드 식별 후 복사본만 `git rm` (로컬 포함 삭제). CI 사고 방지 최우선 |
| **D** | `frontend/dist/assets/` | 🟠 중상 | `.gitignore` + `git rm -r --cached`. B 와 동일 패턴 |
| **E** | `patch_dashboard.py` | 🟡 중 | 이미 DEPRECATED 경고 있음. 물리 삭제 or archive 폴더 이동 |
| **F** | `autopus-adk-main.zip` | 🟠 중상 | 원본 ZIP 불필요. `git rm` (로컬 포함 삭제). git LFS 도입은 과함 |
| **G** | `.minimax/skills/` 대량 | 🟡 중 | `.gitignore` + `git rm -r --cached`. B 와 동일 패턴 |

### 🎯 Phase D-2 권장 실행 체인

**체인**: architect → planner → executor (반복) → reviewer

**이유**: 항목별 처리 방식이 다름 (rm vs rm --cached), Windows 복사본 식별은 판단 필요, `.gitignore` 전체 구조 재설계 병행 여부 결정 필요

### ⚠️ Phase D-2 시작 전 반드시 확인할 것

1. **C (Windows `(2)` 복사본) 의 "진짜 vs 가짜" 판별**
   - 파일 크기, 수정 시각, import 관계 대조
   - `backend/app/main.py` 와 `backend/app/main (2).py` 중 어느 게 FastAPI 진입점인지
   - **실수 시 프로젝트 빌드 불가 수준 손상** → architect 필수

2. **D (빌드 산출물) 와 .gitignore 전체 재설계 병행 여부**
   - 현재 `.gitignore` 는 `.autopus/cache/`, `.mcp.json`, `.agent/browser/`, `.claude/settings.local.json` 등 파편화됨
   - D 처리하면서 한 번에 구조 정리할지 (**권장**) vs D 만 추가할지 결정
   - 전체 재설계 시 커밋 분리 전략: `refactor(gitignore): reorganize ignore patterns by domain` + 각 항목별 `chore(...)`

3. **E (`patch_dashboard.py`) 처리 원칙**
   - 이미 DEPRECATED 경고 삽입됨 → 삭제해도 안전
   - `_deprecated/` 폴더 이동 vs 물리 삭제 선택
   - 아키텍트 잠정 의견: **물리 삭제** (git history 에 있음, archive 불필요)

4. **F (`autopus-adk-main.zip`) 처리**
   - ADK 는 이미 설치 완료 상태 → 원본 ZIP 불필요
   - 물리 삭제 권장
   - 만약 재설치 대비 원본 필요하면 `_handover/` 로 이동 (repo 밖)

5. **Pre-commit hook 으로 재발 방지 설계**
   - `.agent/`, `*/dist/`, `*(2).*`, `*.zip` 등 패턴 감지 hook
   - architect 단계에서 B 처리 패턴 반영해서 종합 설계

### 💡 Phase D-2 모델 선택 권장

**Opus 4.6 권장** (Complex 난이도):
- C 의 "진짜 vs 가짜" 판별 = 판단 복잡도 높음
- `.gitignore` 전체 재설계 = 구조적 설계 작업
- Pre-commit hook 설계 = 방어 로직 설계

단순 실행 (executor) 단계 진입 시 `/model sonnet` 전환.

### 📦 Phase D-2 예상 커밋 수

**최소 3 ~ 최대 5 커밋** 예상:
1. `refactor(gitignore): reorganize ignore patterns by domain` (전체 구조 재설계 시)
2. `chore(cleanup): remove Windows duplicate files` (C 항목)
3. `chore(gitignore): exclude frontend build artifacts` (D 항목)
4. `chore(cleanup): remove deprecated patch script and ADK zip` (E + F 통합 가능)
5. `chore(gitignore): exclude minimax skills workspace` (G 항목)
6. (선택) `feat(hooks): add pre-commit hook for contamination prevention`

→ **1 세션에 3 커밋 이상은 무리**. 필요시 Phase D-2a / D-2b 로 분할.

---

## 📋 프로젝트 기본 정보

| 항목 | 값 |
|---|---|
| 프로젝트명 | Model Agency Management System |
| 경로 | `F:\Project M\model-agency-system` |
| 기술스택 | FastAPI + React + TypeScript + Electron + SQLite |
| GitHub | https://github.com/iamsangjin76-bot/model-agency-system (Private) |
| 현재 로컬·원격 `master` | **`a72988b3`** |
| 개발 프레임워크 | **Autopus-ADK (필수)** |

---

## 🏗️ 개발 원칙 (확정)

1. **Autopus-ADK 기반 개발** — 모든 새 기능/수정은 ADK 에이전트를 통해
2. **모델 선택** — Sonnet 4.6 기본, Opus 4.6 은 Complex 만
3. **매 작업 지시 전에 모델 추천 먼저 제시**
4. **자동 패치 스크립트 금지** — `patch_*.py` 류 ❌
5. **커밋 전략** — 의미 단위 분리 + Lore 포맷 + `.git/COMMIT_MSG_TMP.txt` 재사용
6. **비개발자 협업 프로토콜** — 한 단계씩, 명령어 포함, 어느 창에서 할지 명시
7. **되돌리기 어려운 명령은 허가 후에만**
8. **컨텍스트 윈도우 관리** — 긴 세션은 인수인계로 분할
9. **Lore hook enum 제약 준수** 🆕 — Confidence/Scope-risk/Reversibility 는 bare enum 만
10. **Worker Contract [금지] 는 절대형으로 작성** 🆕 — 조건부 문구 금지
11. **단계별 안내 규칙** 🆕 — 한 번에 한 단계만 전달, 두 단계 이어서 주면 토큰 낭비

---

## ⚠️ 주의사항 / 알려진 이슈

1. **GitHub 히스토리 민감 파일 잔존** — B 감사로 **민감정보 없음 확정**. 추가 조치 불필요.
2. **대시보드 "데이터 없음"** — 정상. 해결 = 우선순위 8
3. **"최근 활동" 하드코딩** — 해결 = 우선순위 8
4. **Claude Code `PostToolUse:Bash hook error`** — 무시 가능 (`failed to list CI runs: exit status 4`)
5. **PowerShell 커밋 메시지 `.git/COMMIT_MSG_TMP.txt` 방식** — 이번 세션도 안정 작동
6. **되돌리기 어려운 명령은 허가 후** — Phase D-2 의 `rm` (물리 삭제) 명령 특히 주의
7. **`.claude/rules/autopus/` 는 gitignore 유지** — ADK 자동 관리 구역
8. **manifest 4파일 churn 관찰** — 이번 세션 변동 없음
9. **`structure.md` 의 `scenarios.md`/`canary.md` 참조 깨짐** — 우선순위 10
10. **인수인계 문서는 `F:\Project M\_handover\` 에 보관** — repo 루트 오염 방지
11. **repo 오염 5항목(C~G) 미처리** 🆕 — Phase D-2 전체 주제
12. **세션 길이 — 1 커밋 + 4 에이전트 호출이 쾌적 한계** 🆕
13. **Lore hook enum 제약** 🆕 — bare 값만. Contract 작성 시 주의
14. **Worker Contract [금지] 절대형 작성** 🆕 — 조건부는 해석 여지 발생

---

## 💡 도구 설정

| 도구 | 기본 모델 |
|---|---|
| claude.ai 채팅창 | Opus 4.6 (아키텍트) |
| Claude Code CLI | **Sonnet 4.6 기본** / Phase D-2 는 **Opus 4.6 권장** |

---

## 📂 인수인계 문서 관리

### 저장 위치
- **경로**: `F:\Project M\_handover\`
- **파일명 규칙**: `인수인계_문서_{YYYYMMDD}_v{N}.md`

### v8 배치
1. **로컬**: `F:\Project M\_handover\인수인계_문서_20260416_v8.md` 로 저장
2. **claude.ai 프로젝트**: v8 파일 업로드 (새 세션 첨부용)
3. **선택사항**: v2~v5 는 프로젝트 지식에서 제거 가능 (v7, v8 있으면 맥락 복원 충분)

---

## 🔧 새 창에서 바로 해야 할 일

### Step 1 — 관리자 PowerShell 1개 + Claude Code CLI

```powershell
cd "F:\Project M\model-agency-system"
claude --model opus
```

> Phase D-2 는 Opus 4.6 기본. 단순 실행 구간 진입 시 `/model sonnet` 전환.

### Step 2 — 아키텍트 Claude 첫 메시지

**첨부 필수**:
1. 이 문서 (`인수인계_문서_20260416_v8.md`)
2. (선택) `인수인계_문서_20260416_v7.md` — Phase D-1 배경 맥락 필요시

**메시지 (복사용)**:

```
나는 비개발자이고 Claude Code CLI로 model-agency-system 프로젝트를 개발 중이야.
항상 한 단계씩, 명령어 포함해서 자세히 설명해줘. 두 단계 이어서 주지 마.
모든 개발은 Autopus-ADK 기반으로 진행해.
매 작업 지시 전에 Sonnet/Opus 중 어느 쪽을 쓸지 먼저 판단해서 알려줘.

이전 세션에서 Phase D-1 B 항목 완전 종료했어 (a72988b3 push 완료, repo clean).
다음 단계는 Phase D-2 — C~G 5항목 통합 설계야.

인수인계 v8 에 Phase D-2 대상 항목, 권장 에이전트 체인, Phase D-1 교훈
(Lore hook enum 제약, Worker Contract 절대형 작성) 정리돼있어.

첫 단계로 architect 에이전트에게 C~G 5항목 통합 설계를 의뢰할
Worker Contract 4줄을 만들어줘. 실행은 내가 CLI 에 붙여넣을 거야.
```

### Step 3 — 첫 행동 권장

**Phase D-2 첫 단계**: architect 에이전트로 C~G 통합 설계
- 각 항목별 처리 방식 결정
- `.gitignore` 전체 재설계 여부 결정
- 커밋 분할 전략 수립
- Pre-commit hook 도입 여부 결정

아키텍트가 Worker Contract 초안 작성 → 사용자 승인 → CLI 실행 순서.

---

## 🏁 오늘 세션 종료 체크리스트

- [x] Phase D-1 B 항목 정찰 (32개 파일 식별)
- [x] security-auditor 텍스트 감사 (민감정보 0건)
- [x] Vision 이중 검증 (PNG 4개 clean)
- [x] executor 추적 해제 + Lore 커밋 `a72988b3`
- [x] push 완료 (origin/master 동기화)
- [x] push 경위 규명 (자동 메커니즘 없음 확정)
- [x] Lore hook enum 제약 교훈 기록
- [x] Worker Contract [금지] 절대형 원칙 정립
- [x] v8 인수인계 문서 작성 — **바로 이 문서**
- [ ] v8 을 `F:\Project M\_handover\` 에 저장 (사용자 작업)
- [ ] v8 을 claude.ai 프로젝트 파일에 업로드 (사용자 작업)
- [ ] Claude Code CLI 세션 종료 (선택)
- [ ] claude.ai 이 채팅창 닫기 (새 세션은 Phase D-2 시작)

---

> **최종 작성**: 2026-04-16 (Phase D-1 B 완전 종료 시점)
> **실행 재개 시점**: 새 세션에서 "architect 로 C~G 통합 설계" 가 첫 액션
> **수고하셨습니다. 🐙**