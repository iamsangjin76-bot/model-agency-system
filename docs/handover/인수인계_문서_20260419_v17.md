# 🐙 Model Agency System — 작업 인수인계 문서 v17

작성일: 2026-04-19 (Phase J-6 통합 검증 진행 중 세션 저장 시점)
이전 문서: `인수인계_문서_20260419_v16.md`

---

## 🎯 이번 세션 한 줄 요약

> **"J-6 통합 검증 Call-1 + Call-1b 완료. PASS 36 / FAIL 2 / WARN 4. fix 범위 확정됨. Jin 시각 2건(2-9, 3-8) 확인 후 Call-4(executor fix) → Call-2(reviewer) → 커밋 진행 예정. 아직 커밋·push 없음, working tree clean 상태 유지."**

---

## 📍 현재 git 상태 — 🟢 완전 동기화 (변경 없음)

### ✅ HEAD = origin/master 일치

```
43c19dbd
```

**working tree: clean**. 이번 세션은 검증만 수행, 소스 코드 수정 없음.

계획서 파일만 생성/추가: `.autopus/brainstorms/J-6-verification-plan.md` (untracked, 로컬 전용)

---

## ✅ 이번 세션에서 완료한 작업

### 1. Phase J-6 통합 검증 개시
- 아키텍트가 전체 미해결 작업(J-6, J-7, J-8, SNS-1, K-1, I-1) 중 **J-6 우선**으로 권장
- 근거: "검증되지 않은 코드 위에 새 기능을 쌓지 않는다" — J-1~J-5 안정성 확정 후 J-8, J-7 진입
- Jin 동의 → J-6 → J-8 → J-7 순서 확정

### 2. planner 호출 → J-6 검증 계획서 작성
- 파일: `.autopus/brainstorms/J-6-verification-plan.md` (147줄)
- Worker Contract 4항목 (목표/산출물/완료 기준/금지) 완비
- 3주체(executor/reviewer/Jin) 분리
- 검증 축 4개 (뉴스 파이프라인 / 이미지 파이프라인 / 다크모드 / 에러 시나리오)
- 항목별 executor 자동 / reviewer 판정 / Jin 시각 3열 체크박스 테이블
- 아키텍트 검토 결과: API 경로·컴포넌트 경로·인증 구조 추정 부분이 있었으나 executor Call-1에 교정 지시 주입으로 보정

### 3. executor Call-1 자동 검증 수행
- API 실경로 확인 후 계획서 교정하며 진행
- 정적 코드 검증 (grep 중심) + curl 엔드포인트 존재 확인
- **결과**: PASS 30 / FAIL 2 / WARN 3 / Jin 보류 9

### 4. executor Call-1b 보충 자동 검증 수행 (Jin 시각 목록 축소 조치)
- Jin 지적: "Claude가 자체적으로 더미 생성 테스트 할 수 있는 거 아니야?" → planner가 시각 항목을 과잉 분류한 점 확인
- curl로 자동화 가능한 7개 항목(1-5, 1-6, 1-10, 1-12, 2-6, 2-11, 4-10)을 executor로 이관
- **end-to-end 실 데이터 테스트 성공**:
  - 테스트 모델 생성 (id=19) → 뉴스 저장(HTTP 201) → 조회(2건 확인) → 개별 삭제 → 재조회 부재 확인
  - 테스트 데이터 cleanup 완료 (news/10, news/11 hard delete, models/19 soft delete)
- **결과**: PASS 6 / WARN 1 (2-11은 placeholder URL 한계로 스킵)

### 5. 통합 집계
- **PASS 36 / FAIL 2 / WARN 4 / Jin 시각 보류 2**
- Jin 보류는 9건 → 2건으로 축소 (2-9, 3-8만 시각 확인 필요)

---

## 🔴 지금 멈춘 지점

**Jin 시각 2건(2-9, 3-8) 확인 직전**에서 세션 종료.

이후 계획:
1. Jin 시각 확인 (2-9, 3-8)
2. Call-4 executor fix (3건 통합)
3. Call-2 reviewer 최종 판정
4. Lore 커밋 + push (Jin 허가 후)

---

## 🎯 확정된 fix 범위 (아키텍트 최종 분류)

| # | 항목 | 심각도 | 본 세션 fix | 이유 |
|---|---|---|---|---|
| **1-2** | Google provider 500 에러 전파 → graceful 4xx 처리 필요 | 🟡 Medium | ✅ 포함 | 백엔드 에러 처리 누락 |
| **3-5** | ImagePreviewModal.tsx 다크모드 클래스 전무 | 🟠 High | ✅ 포함 | UX 직접 영향 |
| **3-10** | ModelNewsList, ModelImageGallery `dark:border-` 누락 | 🟢 Low | ✅ 포함 | 3-5와 다크모드 한 커밋으로 묶기 |
| 2-12 | toast.warning optional chaining (ToastContext에 warning 없을 가능성) | 🟢 Low | ❌ 스킵 | 런타임 영향 없음, 별도 UX 세션 |
| 4-8 | AbortController/timeout 없음 | 🟢 Low | ❌ 스킵 | 현재 블로커 아님, 별도 UX 세션 |

**예상 변경 파일 수**: 3~4개
- `backend/app/routers/news.py` 또는 `backend/app/services/search_service.py` (Google 500 → 4xx)
- `frontend/src/components/...ImagePreviewModal.tsx` (다크모드 전체 추가)
- `frontend/src/components/.../ModelNewsList.tsx` (dark:border-)
- `frontend/src/components/.../ModelImageGallery.tsx` (dark:border-)

---

## 👤 Jin 시각 확인 잔여 (다음 세션 첫 단계)

### 2-9. 포트폴리오 등록 UI 존재 여부
- 모델 상세 페이지 > 이미지 갤러리 > 이미지 하나에 "포트폴리오 등록" 버튼/토글 보이는지
- 버튼 존재만 확인 (실제 등록 동작은 코드 PASS로 검증됨)

### 3-8. 다크모드 1~4번 화면 텍스트 가독성
- 헤더 다크모드 토글 클릭
- 뉴스 검색 페이지, 이미지 검색 페이지, 모델 상세 > 뉴스 섹션, 모델 상세 > 이미지 탭 순서로 훑기
- 텍스트가 어둠 속에 묻히는 곳 있는지만 확인 (5번 프리뷰 모달은 FAIL 확정이라 스킵)

**보고 형식**: `2-9: PASS / 3-8: PASS` 또는 FAIL 경우 한 줄 코멘트

---

## 🗺️ 전체 프로젝트 진행 현황 (v17)

| Phase | 이름 | 상태 |
|-------|------|------|
| A~D | ADK 세팅 + repo 정리 | ✅ 완료 |
| E | 데이터 기반 구축 | ✅ 완료 |
| F | 핵심 도메인 CRUD | ✅ 완료 |
| G-1 | 알림 시스템 | ✅ 완료 |
| G-2 | 파일 업로드 | ✅ 완료 |
| G-3 | 검색·필터·정렬 | ✅ 완료 |
| H-1 | 반응형 레이아웃 | ✅ 완료 |
| H-2 | 다크모드 | ✅ 완료 |
| H-3 | 에러 처리·로딩 UX | ✅ 완료 |
| J-1 | 검색 백엔드 인프라 | ✅ 완료 |
| J-2 | 뉴스 라우터 API | ✅ 완료 |
| J-3 | 이미지 라우터 API | ✅ 완료 |
| J-4 | 프론트엔드 API 연동 | ✅ 완료 |
| J-5 | 모델 상세 + 뉴스/이미지 섹션 | ✅ 완료 |
| **J-6** | **통합 검증 + fix** | **🟡 진행 중 (PASS 36, FAIL 2, WARN 4 → fix 대기)** |
| **J-8** | **이미지 검색 UX 개선** | **🔴 미착수 (J-6 이후)** |
| **J-7** | **라이브러리 관리 뷰** | **🔴 미착수 (J-8 이후)** |
| SNS-1 | SPEC-SNS-001 설계 + 구현 | 🔴 미착수 |
| K-1 | 프로필 내보내기 템플릿 4종 | 🔴 미착수 |
| I-1 | Electron 패키징 | 🔴 미착수 |
| I-2 | 백업·복원 | 🔴 미착수 |
| I-3 | 테스트 기반 구축 | 🔴 미착수 |

---

## 🎓 이번 세션에서 얻은 교훈 🆕

### 1. planner가 시각 항목을 과잉 분류하는 경향
- Jin이 "Claude가 자체적으로 더미 생성 테스트 할 수 있는 거 아니야?" 지적
- 9건의 Jin 시각 항목 중 7건이 실은 curl + grep으로 자동화 가능했음
- **교훈**: planner 계획서 검토 시 아키텍트가 "이 중 자동화 가능한 게 뭐지?"를 선제적으로 물어야 함. 시각 목록은 최후의 것만 남겨야 함.
- **v16 원칙 33 신설 후보**: planner 계획 검토 시 Jin 시각 항목을 "이게 정말 자동화 불가능한가?" 기준으로 재분류

### 2. 아키텍트 모델 판단의 명시 방법
- Jin이 두 차례 지적: "모델 선택 지시 해야지" / "어느 모델이 적합한지 판단을 하라는 거야"
- 아키텍트가 본문에 작게 적는 것으로는 부족, **호출 지시 최상단에 "🎯 이번 작업 적합 모델: Sonnet (근거: ...)"** 한 줄로 먼저 선언해야 함
- **v16 원칙 34 신설 후보**: 에이전트 호출 지시 최상단에 모델 판단 1줄 고정 블록

### 3. 순서 재조정은 Jin 동의 후 진행
- WORKFLOW_v3 Medium 정석: planner → executor → reviewer
- 이번 J-6은 검증 특성상 Jin 시각이 미지수라 reviewer 전에 Jin 시각을 먼저 확정하는 게 효율적
- 경로 B(순서 조정)는 Jin 동의 받고 진행 → 표준과 다를 때는 선택지 제시 원칙 유지

### 4. 검증 세션도 스코프 경계 엄수 경향
- Call-1 + Call-1b + (Call-3 Jin 시각) + Call-4 + Call-2 = 총 5회 예상
- WORKFLOW_v3 권장 3~4회를 초과하나, 검증 세션 특성상 허용 범위
- 단, 다음 세션에서 fix 완료 후 추가로 새 기능 진입하지 말 것 (세션 분할 권장)

---

## 🏗️ 개발 원칙 (v17 갱신 후보)

v16 기존 32개 원칙 유지 + 아래 추가 후보:

33. **planner 시각 항목 과잉 분류 방지** 🆕 — planner 계획서 검토 시 Jin 시각 항목을 "이게 정말 자동화 불가능한가?" 기준으로 재분류. curl/grep/스크립트로 대체 가능한 것은 executor로 이관.
34. **아키텍트 모델 판단 명시 블록** 🆕 — 에이전트 호출 지시 최상단에 `🎯 이번 작업 적합 모델: Sonnet|Opus (근거: ...)` 한 줄 고정 블록. 본문 속 언급만으로는 Jin이 놓칠 수 있음.

---

## 💡 도구 설정 (v17, 변경 없음)

| 도구 | 기본 모델 |
|---|---|
| claude.ai 채팅창 | Opus 4.6 (아키텍트) |
| Claude Code CLI | **Sonnet 4.6 기본** / Complex 작업은 Opus 4.6 |

---

## 🔧 새 창에서 바로 해야 할 일

### Step 1 — 관리자 PowerShell + Claude Code CLI

```powershell
cd /d "F:\Project M\model-agency-system"
claude --model sonnet
```

### Step 2 — 서버 실행 (Jin 시각 확인용)

**창 A (백엔드)**:
```powershell
cd "F:\Project M\model-agency-system\backend"
.\venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload
```

**창 B (프론트엔드)**:
```powershell
cd "F:\Project M\model-agency-system\frontend"
npm run dev
```

### Step 3 — 아키텍트 Claude 첫 메시지

**첨부 필수**: `인수인계_문서_20260419_v17.md`

**메시지 (복사용)**:

```
나는 비개발자이고 Claude Code CLI로 model-agency-system 프로젝트를 개발 중이야.
항상 한 단계씩, 명령어 포함해서 자세히 설명해줘. 두 단계 이어서 주지 마.
모든 개발은 Autopus-ADK 기반으로 진행해.
매 작업 지시 전에 어느 모델(Sonnet/Opus)이 적합한지 판단해서 근거와 함께 알려줘.

이전 세션에서 J-6 통합 검증 Call-1 + Call-1b 완료 상태야.
인수인계 v17 첨부했으니 맥락 확인하고,
Jin 시각 2건(2-9, 3-8) 확인부터 이어서 진행해줘.
```

### Step 4 — 다음 세션 진행 순서

| 순서 | 작업 | 난이도 | 적합 모델 |
|------|------|--------|----------|
| 1 | Jin 시각 확인 (2-9, 3-8) | - | Jin 본인 |
| 2 | Call-4 executor fix (3건 통합) | 🟡 Medium | Sonnet |
| 3 | Call-2 reviewer 최종 판정 | 🟡 Medium | Sonnet |
| 4 | Lore 커밋 + push | Simple | Sonnet |
| 5 | (다음 세션 분할) J-8 이미지 검색 UX 개선 | 🟡 Medium | Sonnet |
| 6 | (다음) J-7 라이브러리 관리 뷰 | 🔴 Complex | Opus → Sonnet |

---

## ⚠️ 주의사항 / 알려진 이슈 (v17 유지)

v16 39~43번 유지. 이번 세션 신규 발견 이슈:

44. **Google Custom Search 500 에러 전파** 🆕 — provider=google 요청 시 서버가 HTTP 500 반환. 4xx graceful 처리 필요 (J-6 fix 1-2 대상)
45. **ImagePreviewModal 다크모드 전무** 🆕 — dark: 클래스 0개. 5개 컴포넌트 중 유일하게 다크모드 미지원 (J-6 fix 3-5 대상)
46. **ModelNewsList/Gallery dark:border- 누락** 🆕 — 배경·텍스트 dark: 클래스는 있으나 border-만 누락 (J-6 fix 3-10 대상)
47. **toast.warning optional chaining** 🆕 — ToastContext에 warning 메서드 미정의 가능성. optional chaining으로 런타임 에러는 안 나지만 WARN 토스트 미출력 가능. (별도 UX 세션 대상, 본 J-6 범위 아님)
48. **AbortController/timeout 패턴 부재** 🆕 — 장시간 요청 시 타임아웃 피드백 없음. (별도 UX 세션 대상, 본 J-6 범위 아님)

---

## 🏁 이번 세션 종료 체크리스트

- [x] J-6 검증 계획서 작성 (planner)
- [x] Call-1 자동 검증 (PASS 30 / FAIL 2 / WARN 3)
- [x] Call-1b 보충 자동 검증 (PASS 6 / WARN 1, Jin 시각 9→2로 축소)
- [x] fix 범위 확정 (3건: 1-2, 3-5, 3-10)
- [x] 테스트 데이터 cleanup 완료 (models/19 soft, news/10·11 hard delete)
- [x] v17 인수인계 문서 작성 — **바로 이 문서**
- [ ] v17을 `F:\Project M\_handover\`에 저장 (Jin 작업)
- [ ] v17을 claude.ai 프로젝트 파일에 업로드 (Jin 작업)

---

## 📂 보존 파일

- `.autopus/brainstorms/J-6-verification-plan.md` — 계획서 + Call-1/1b 결과 append (다음 세션 Jin 시각 결과 + Call-4 결과도 append 예정)
- 테스트 데이터: 모두 cleanup 완료. 잔존 오염 없음.

---

> **최종 작성**: 2026-04-19 (J-6 Call-1+1b 완료 시점, Jin 시각 대기 중 세션 저장)
> **실행 재개 시점**: 새 세션에서 Jin 시각 2건 확인부터 → Call-4 executor fix → Call-2 reviewer → 커밋 push
> **수고하셨습니다. 🐙**