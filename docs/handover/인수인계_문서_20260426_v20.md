# 🐙 Model Agency System — 작업 인수인계 문서 v20

작성일: 2026-04-26 (J-6 완전 종결 시점)
이전 문서: `인수인계_문서_20260425_v19.md`

---

## 🎯 이번 세션 한 줄 요약

> **"J-6 ②③ 자동 검증 8항목(A1~A4 + V1~V4) 모두 PASS/PARTIAL 종결. 코드 변경 0건, Jin 시각 검증 0회 달성. 누적 문서 정정 4건 + 신규 운영 원칙 R37/R39 사례 확정. 다음 세션 J-8 진입."**

---

## 📍 현재 git 상태 — 🟢 완전 동기화

```
99253666  fix(search): graceful 503 conversion for direct google/naver call branches  ← HEAD = origin/master
2a1a1524  docs(setup): finalize 6 context documents with 30 precision corrections
43c19dbd  fix(news-search): auto-match model from query and auto-create if missing
```

**local HEAD = origin/master = `99253666`**. branch ahead/behind 0. working tree clean. **본 세션 코드 변경 0건**.

---

## ✅ 이번 세션 완료 — J-6 자동 검증 8항목 (R40 묶음 위임)

### 1라운드: A1~A4 (정찰 + curl 검증)

| 항목 | 결과 | 핵심 |
|---|---|---|
| A1 DashboardHome 정적 | 🟡 PARTIAL | 파일명 정정 — `DashboardHome.tsx` ❌ → `DashboardPage.tsx:96 내부 함수` ✅. 라벨 4 + 차트 import/JSX 모두 PASS |
| A2 Auth+KPI API curl | ❌→✅ | endpoint 정정 — `/api/dashboard/kpi` ❌ → `/api/stats/dashboard` ✅. 200 + 4 KPI 필드 정상 |
| A3 silent refresh | ✅ PASS | `/api/auth/refresh` 200 + access/refresh 토큰 rotation 정상 |
| A4 ImagePreviewModal 정적 | ✅ PASS | bg-black/90 1회 + text-white 5회 + dark: 0건 → Fix 3-5 폐기 일관성 확정 |

### 2라운드: V1~V4 (정적 + tsc + git log)

| 항목 | 결과 | 핵심 |
|---|---|---|
| V1 콘솔 에러 정적 | ✅ PASS | DashboardHome `console.error`/`throw` 0건 + `npx tsc --noEmit` exit 0 (모든 import 정상 해소) |
| V2 다크모드 토글 | 🟡 PARTIAL | 인프라 정상(ThemeToggle/ThemeContext + tailwind class 전략 + 46 dark: 페어). DashboardPage.tsx:278 PlaceholderPage 1줄만 다크 짝 부재 (Low) |
| V3 레이아웃 정적 | ✅ PASS | KPI grid 반응형 1→2→4 + 차트 3개 모두 ResponsiveContainer wrap |
| V4 Lightbox 인용 | ✅ PASS | 99253666 이후 변경 0 → A4 결과 그대로 유효 |

### 핵심 성과

- **자동 검증으로 시각 회귀 항목 종결** — Jin 본인 시각 검증 요청 0회 (R40 정신 충실)
- **2 PARTIAL 본질** — 모두 J-6 결격 사유 아님, J-8 통합 처리 권장
- **executor 자체 정찰 정정 2건** — A1 파일 경로 + A2 endpoint (R37 강력한 근거 사례 추가)
- **read-only 검증 100%** — 코드 변경 0, 신규 패키지 0, 추측 0

---

## 🆕 J-8 통합 처리 항목 (3건)

J-8(이미지 검색 UX + 외부 이미지 프록시) 진입 시 함께 처리할 항목 정리:

### 통합 1. 외부 이미지 핫링크 차단 (v19 side-issue #1, 🟡 Medium)

- 증상: `imgnews.naver.net` / `i1.ruliweb.com` 등 외부 CDN 이미지가 `<img>` 태그에서 로드 실패
- 카드 메타데이터(도메인 라벨, 해상도)는 표시되나 실제 이미지는 회색 placeholder
- **권장 해법**: 백엔드 프록시 `/api/proxy/image?url=` 도입 (Referer 헤더 우회 + 캐싱)
- 영향: `frontend/src/components/search/*` 이미지 카드 컴포넌트 + `backend/app/api/` 신규 라우터

### 통합 2. PlaceholderPage 다크 짝 부재 (v20 V2 PARTIAL, 🟢 Low)

- 위치: `frontend/src/pages/DashboardPage.tsx:278` `function PlaceholderPage()`
- 현재: `<div className="bg-white rounded-2xl p-8 shadow-sm border border-gray-100">`
- 권장: `<div className="bg-white dark:bg-gray-800 rounded-2xl p-8 shadow-sm border border-gray-100 dark:border-gray-700">`
- 사용처: `<Route path="share">` + `<Route path="settings">` (둘 다 미구현 라우트)
- **주의**: PlaceholderPage 자체가 임시 컴포넌트라 Share/Settings 실 구현 시 자동 폐기 가능 → ARCHITECTURE.md Known Issue #14와 동일 영역에서 함께 처리

### 통합 3. 통합 KPI vs 도메인별 분산 호출 정책 (v20 신규 발견, 🟢 Low)

- 백엔드 `/api/stats/dashboard`는 통합 KPI 4필드를 한 번에 반환하지만, frontend는 `modelsAPI.stats()` + `castingsAPI.stats()` + `settlementsAPI.stats()` 3회 분산 호출 중
- 위치: `DashboardPage.tsx:106 / 118 / 134`
- 현재 작동 정상, 단순 비효율
- **결정 필요**: 통합 endpoint로 일원화 vs 도메인별 분산 유지 (J-7/J-8 검토 안건)

---

## 🆕 v20 신규 운영 원칙 확정 (R37·R39 강력 근거 사례 추가, R38·R40 유지)

| # | 원칙 | v20 사례 |
|---|---|---|
| **R37 ✅** | autopus 자체 발견 오류 즉시 정정 | **본 세션 결정적 검증** — A1 파일 경로(`DashboardHome.tsx` → `DashboardPage.tsx:96`) + A2 endpoint(`/api/dashboard/kpi` → `/api/stats/dashboard`) 모두 executor 자체 정찰로 발견·정정. 아키텍트 추측 기반 Worker Contract를 코드 직접 읽기로 무력화 |
| R38 | 카운트 표기에 산수식 명시 | v20 사례 0건 (후보 유지) |
| **R39 ✅** | setup·검증 시 코드 직접 읽기 의무 | **본 세션 결정적 검증** — V1 import 추정 무력화는 `npx tsc --noEmit exit 0` 결정적 출력으로 확정. 추측 0건 달성의 핵심 |
| **R40 ✅** | 사용자 결정 분기점만 멈춤 | **본 세션 적용** — 8항목 검증을 2 라운드 묶음 위임으로 처리, Jin 시각 확인 0회. 단 1라운드에서 V1~V4 명세를 사람 시각으로 잘못 안내한 회귀 발생 → Jin 즉시 지적 → 2라운드 자동 검증 묶음으로 정정 |

### v20 신규 후보: R41 (회귀 방지)

| # | 원칙 | 근거 |
|---|---|---|
| R41 🆕 | 아키텍트는 **자동화 가능 항목을 사람 시각 항목으로 분류 금지**. 분류 전 "코드/빌드/grep/curl로 자동 판정 가능한가" 자가 점검 의무 | 본 세션 V1~V4 1차 안내에서 자동화 가능 항목 4개 모두 사람 시각으로 분류 → Jin 지적 → 2차 자동 검증으로 종결. R40의 구체 보강 |

다음 세션에서 Jin 동의 시 v17 36개 + R37·R39·R40·R41 = 40개로 확정 갱신 (R38은 후보 유지).

---

## 🗺️ 전체 Phase 진행 현황 (v20)

| Phase | 이름 | 상태 |
|---|---|---|
| A~D | ADK 세팅 + repo 정리 | ✅ 완료 |
| E~G | 시드/CRUD/알림 | ✅ 완료 |
| H | 반응형/다크모드/에러 UX | ✅ 완료 |
| J-1~J-5 | 검색 인프라 + 라우터 + 프론트 + 모델 상세 통합 | ✅ 완료 |
| /auto setup | 6개 컨텍스트 문서 + 30건 보정 | ✅ v18 |
| J-6 | 통합 검증 + Fix 1-2 + ②③ 자동 검증 | ✅ **v20 완전 종결** |
| **J-8** | **이미지 검색 UX + 외부 이미지 프록시 (통합 1·2·3 포함)** | 🔴 **다음 세션 진입** |
| J-7 | 라이브러리 관리 뷰 | 🔴 미착수 (Complex, Opus 권장) |
| K-1 | 프로필 내보내기 4종 | 🔴 미착수 |
| SNS-1 | SPEC-SNS-001 | 🔴 미착수 |
| I-1~I-3 | Electron + 백업 + 테스트 | 🔴 미착수 |

---

## 🔧 다음 세션 시작 절차

### Step 1 — 서버 실행 (이미 켜져 있으면 스킵)

```powershell
# 창 1: Backend
cd "G:\Project M\model-agency-system\backend"
.\venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload

# 창 2: Frontend
cd "G:\Project M\model-agency-system\frontend"
npm run dev

# 창 3: Claude Code CLI
cd "G:\Project M\model-agency-system"
claude --model sonnet
```

### Step 2 — 새 claude.ai 아키텍트 대화 첫 메시지

**첨부 필수**: `인수인계_문서_20260426_v20.md` (본 문서)

```
나는 비개발자이고 model-agency-system 프로젝트를 개발 중이야.
항상 한 단계씩, 명령어 포함해서 자세히 설명해줘. 두 단계 이어서 주지 마.
모든 개발은 Autopus-ADK 기반으로 진행해.
매 작업 지시 전에 Sonnet/Opus 중 어느 쪽을 쓸지 먼저 판단해서 알려줘.
존댓말 사용해줘.
사용자 결정 분기점만 멈추고, 단순 검증·실행은 autopus 묶음 위임으로 처리해줘 (R40).
자동화 가능 항목을 사람 시각 항목으로 분류하지 마 (R41).

이전 세션(2026-04-26)에서 J-6 완전 종결.
v20 첨부했어. J-8 (이미지 검색 UX + 외부 이미지 프록시) 진입해줘.
J-8에는 통합 처리 항목 3건 포함:
1. 외부 이미지 핫링크 차단 → 백엔드 /api/proxy/image 프록시 도입
2. PlaceholderPage 278줄 다크 짝 부재 (1줄, Low)
3. 통합 KPI vs 도메인별 분산 호출 정책 결정

먼저 J-8 SPEC 설계부터 시작해줘.
```

### Step 3 — J-8 진입 권장 흐름

| 순서 | 작업 | 난이도 | 모델 |
|---|---|---|---|
| 1 | J-8 SPEC 설계 (외부 프록시 + UX 패리티 + 통합 항목 3건) | Medium | Sonnet 4.6 (설계 복잡 시 Opus 4.6 검토) |
| 2 | planner → executor → reviewer 순서 (WORKFLOW_v3 Medium) | Medium | Sonnet 4.6 |
| 3 | security-auditor (외부 URL 프록시 보안 검토 필수 — SSRF 방지) | Medium | Sonnet 4.6 |
| 4 | commit + push | Simple | executor |

### J-8 보안 사전 메모 (security-auditor 필수)

외부 이미지 프록시는 SSRF(Server-Side Request Forgery) 위험이 큼 — 다음 항목 필수 검토:

- 허용 도메인 화이트리스트 (`imgnews.naver.net`, `i1.ruliweb.com` 등 검색 결과 도메인만)
- private IP 대역 차단 (`127.0.0.1`, `10.0.0.0/8`, `192.168.0.0/16` 등)
- 응답 크기 제한 (예: 10MB)
- timeout 설정 (예: 5초)
- Content-Type 화이트리스트 (`image/*`만)
- 캐싱 전략 (디스크 vs 메모리 vs CDN)

---

## 🏁 이번 세션 종료 체크리스트

- [x] J-6 ②③ 자동 검증 8항목 모두 종결 (3 PASS + 1 PARTIAL × 2 = 6 PASS + 2 PARTIAL)
- [x] 코드 변경 0건 / Jin 시각 검증 0회 달성
- [x] 누적 문서 정정 4건 (A1 파일 경로 + A2 endpoint + V2 PartialPage 메모 + 통합 KPI 정책)
- [x] R37·R39 강력 근거 사례 확정 + R41 후보 신설
- [x] J-8 통합 처리 항목 3건 정리
- [x] J-8 보안 사전 메모 작성 (SSRF 방지 6항목)
- [x] v20 인수인계 문서 작성 — **본 문서**
- [ ] v20를 `G:\Project M\model-agency-system\handover\`에 저장 (Jin 작업)
- [ ] v20를 claude.ai 프로젝트 파일에 업로드 (Jin 작업)

---

## 📂 본 세션 산출 파일

- (없음 — 본 세션은 검증 전용, 코드 변경 0건)
- `handover/인수인계_문서_20260426_v20.md` — **본 문서** (Jin 저장 예정)

---

## 📌 다음 세션 첫 메시지 추천 사용 여부

**Step 2의 메시지 블록을 그대로 복사**하시면 됩니다. v20 첨부 + R41 추가 + J-8 통합 항목 3건 명시 + J-8 SPEC 설계부터 시작 — 이 4가지가 핵심입니다.

---

> **최종 작성**: 2026-04-26 (J-6 완전 종결 시점)
> **실행 재개 시점**: 새 세션에서 v20 첨부 → J-8 SPEC 설계부터
> **수고하셨습니다. 🐙**
