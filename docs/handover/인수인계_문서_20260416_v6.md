# 🐙 Model Agency System — 작업 인수인계 문서 v6

작성일: 2026-04-16 (Phase C 커밋 4 지시 직후, 결과 미확인 상태에서 선제 분할)
이전 문서: `인수인계_문서_20260416_v5.md`

---

## 🎯 이번 세션 한 줄 요약

> **"Phase C 커밋 2.7a/2.7b/3 완료 (3 커밋 추가). 커밋 4 Claude Code 실행 지시 직후, 결과 확인 전에 선제 세션 분할. Push 는 새 세션에서."**

---

## 📍 현재 git 상태

### ✅ 이번 세션에서 추가된 커밋 (3개 확정)

```
2236d5ca docs(workflow): establish non-developer solo-play workflow            ← 커밋 3
2eaf344d chore(autopus): add auto plugin skills and codex plugin registration  ← 커밋 2.7b
156adcd0 docs(autopus): baseline project knowledge and harness manifests       ← 커밋 2.7a
```

### 🟡 이번 세션 마지막 상태 (확인 필요)

**커밋 4** (`fix(templates): add missing A3 branding format canonical source`) 을 Claude Code CLI 에 실행 지시한 직후 세션 분할.

- 대상 파일: `templates/shared/branding-formats.md.tmpl` (신규 생성)
- 목적: `branding.md:63` 및 14개 에이전트가 참조하지만 repo 에 부재한 canonical template 공백 해소 (R7/R9 위반 해소)
- 실행 중 추가 검증 단계: `grep -rn "branding-formats.md.tmpl"` 로 참조 경로 일관성 확인 (경로 불일치면 중단하고 사용자에게 보고하도록 지시됨)

**아키텍트가 결과 확인 못함.** 새 세션 첫 작업 = Claude Code CLI 에 남아있는 커밋 4 결과 보고를 복사해서 아키텍트 Claude 에게 전달.

### 📜 원격 미push 커밋 (이번 세션까지 누적)

| 커밋 해시 | 메시지 | 세션 |
|---|---|---|
| ? (결과 미확인) | fix(templates): add missing A3 branding format canonical source | 이번 세션 |
| `2236d5ca` | docs(workflow): establish non-developer solo-play workflow | 이번 세션 |
| `2eaf344d` | chore(autopus): add auto plugin skills and codex plugin registration | 이번 세션 |
| `156adcd0` | docs(autopus): baseline project knowledge and harness manifests | 이번 세션 |
| `224ca59a` | chore(context): replace go-specific deny patterns with python/react rules | 이전 세션 |
| `f57e06e6` | fix(gitignore): allow .autopus/ tracking for project knowledge | 이전 세션 |
| `a9e6258f` | chore(config): switch commit language policy from en to ko | 이전 세션 |

**원격 최신은 `224ca59a` 이전**일 가능성 높음 (v5 에서도 이전 3 커밋 미push 가능성 언급). 새 세션에서 `git log origin/master --oneline -3` 으로 반드시 확인.

---

## 🔧 새 창에서 바로 해야 할 일

### Step 1 — PowerShell + Claude Code CLI 유지

**Claude Code CLI 는 이전 세션 그대로 살려두세요.** 커밋 4 결과가 거기 남아있습니다. 종료하지 마세요.

새 창은 **claude.ai 아키텍트 Claude 용**:

```powershell
cd "F:\Project M\model-agency-system"
```

(PowerShell 은 이미 열려있으면 그대로)

### Step 2 — 아키텍트 Claude 첫 메시지

**첨부 필수**:
1. 이 문서 (`인수인계_문서_20260416_v6.md`)
2. (선택) `인수인계_문서_20260416_v5.md` — 배경 맥락 필요시

**메시지 (복사용)**:

```
나는 비개발자이고 Claude Code CLI로 model-agency-system 프로젝트를 개발 중이야.
항상 한 단계씩, 명령어 포함해서 자세히 설명해줘.
모든 개발은 Autopus-ADK 기반으로 진행해.
매 작업 지시 전에 Sonnet/Opus 중 어느 쪽을 쓸지 먼저 판단해서 알려줘.

이전 세션에서 Phase C 커밋 3개(156adcd0, 2eaf344d, 2236d5ca) 성공하고
커밋 4 지시 직후 결과 확인 전에 선제 분할했어. Claude Code CLI 는
그대로 살려뒀고 커밋 4 결과가 거기 남아있어. 지금 복사해서 붙여넣을게.

[여기에 커밋 4 결과 전체 복붙 — grep 출력, 커밋 해시, git log, git status 포함]

이어서 push 까지 마무리해줘.
```

### Step 3 — 아키텍트가 판단할 3가지 체크포인트

1. **커밋 4 성공 여부**
   - 커밋 해시 정상 생성
   - 파일 정확히 1개 신규 (`templates/shared/branding-formats.md.tmpl`)
   - pre-commit / commit-msg 훅 통과
   - grep 결과에 경로 불일치 없었는지

2. **Push 조건 충족 여부**
   - `git log origin/master --oneline -3` 결과 확인
   - 로컬-원격 차이 = 최대 7 커밋 (이번 세션 4 + 이전 세션 3)
   - working tree clean 확인

3. **Push 후 정리**
   - `.git/COMMIT_MSG_TMP.txt` 삭제 (재사용 끝)
   - `git status` 로 working tree clean 재확인
   - `git log origin/master --oneline -10` 으로 원격 동기화 최종 검증

---

## ⚠️ 커밋 4 실패 시나리오별 대응 (아키텍트 참고용)

### Case A — grep 경로 불일치로 executor 가 멈춤

executor 가 Step 1 에서 다른 경로 (예: `.autopus/templates/shared/`) 참조를 발견하고 보고했다면:
- **경로 목록을 받아 analyze** → 어느 경로가 canonical 인지 판단
- 후보 1: `templates/shared/` (v5 지시 경로)
- 후보 2: `.autopus/templates/shared/` (ADK 관례)
- 판단 근거: `branding.md:63` 의 실제 문자열 + ADK 하네스 convention
- 필요시 explorer 로 재조사 후 path 수정 지시

### Case B — gitignore 걸림

`templates/` 가 gitignore 에 들어있을 가능성:
- `.gitignore` 점검 → 해당 라인 식별
- `.autopus/` 처럼 `!templates/` 로 예외 허용 추가 필요할 수도
- 이 경우 커밋 4 내용이 **"gitignore 수정 + 파일 생성"** 두 커밋으로 쪼개질 수 있음

### Case C — 훅 실패

Lore 메시지 훅 거부:
- subject line 72자 초과 확인 (제공한 메시지는 65자로 안전 범위)
- 이전 4 커밋이 전부 통과했으므로 훅 자체 동작 문제 가능성 낮음
- 메시지 파일 인코딩 (BOM/CRLF) 점검

---

## 🗺️ Push 완료 후 남은 우선순위 작업

| 순위 | 작업 | 상태 |
|---|---|---|
| ~~1~~~~3~~ | 완료분 (JWT, 대시보드, 저장소 정리, ADK 워크플로) | ✅ |
| **4** | 알림 시스템 | ⏳ **다음 주 작업** |
| 5 | 파일 업로드 검증 | ⏳ |
| 6 | 반응형 / 다크모드 | ⏳ |
| 7 | DB 시드 데이터 | ⏳ |
| 8 | "최근 활동" 실제 DB 연동 | ⏳ |
| 9 | `.claude/settings.local.json` 처리 결정 | ⏳ |
| 10 | `structure.md` 의 `scenarios.md` / `canary.md` 참조 정리 | ⏳ |
| 11 | Lore 훅 강화 (Autopus sign-off + Constraint 필수 검증) | ⏳ 장기 |
| 12 | SPEC-AUTH-002 (JWT storage hardening) | ⏳ |

---

## 🏗️ 개발 원칙 (v5 에서 승계, 변경 없음)

1. **Autopus-ADK 기반 개발** — 모든 새 기능은 ADK 에이전트 경유
2. **모델 선택 — Sonnet 기본, Opus 는 Complex 만** (이번 세션 4 커밋 모두 Sonnet 으로 성공)
3. **매 작업 지시 전에 모델 추천 먼저 제시**
4. **자동 패치 스크립트 금지** (`patch_*.py` 류 ❌)
5. **커밋 전략** — 의미 단위 분리 + Lore 포맷 + `.git/COMMIT_MSG_TMP.txt` 재사용
6. **비개발자 협업 프로토콜** — 한 단계씩, 명령어 포함, 어느 창에서 할지 명시
7. **되돌리기 어려운 명령은 허가 후에만** (특히 push, rm, force flag)
8. **컨텍스트 윈도우 관리 — 긴 세션은 선제적으로 분할** ⚠️ (이번 세션에서 재확인)
9. **존댓말 사용**

---

## ⚠️ 주의사항 / 알려진 이슈

1. **GitHub 히스토리 민감 파일 잔존** — 변경 없음
2. **대시보드 "데이터 없음"** — 정상 (해결 = 우선순위 8)
3. **"최근 활동" 하드코딩** — 해결 = 우선순위 8
4. **Claude Code `PostToolUse:Bash hook error`** — 무시 가능 (이번 세션도 반복 확인, `failed to list CI runs: exit status 4`)
5. **PowerShell 커밋 메시지 `.git/COMMIT_MSG_TMP.txt` 방식** — 이번 세션 3 커밋 전부 성공. 프로토콜 확립됨.
6. **되돌리기 어려운 명령은 허가 후** — 재강조
7. **`.claude/rules/autopus/` 는 gitignore 유지** — ADK 자동 관리 구역
8. **manifest 4파일 churn 관찰 시작** — 커밋 2.7a 후 모니터링 대상
9. **`structure.md` 의 `scenarios.md`/`canary.md` 참조 깨짐** — 우선순위 10
10. **`인수인계_문서_20260416_v*.md` untracked 누적** 🆕 — v2/v3/v4/v5/v6 전부 프로젝트 루트에 로컬 파일로만 존재. 별도 폴더 이동 or 정리 필요. 우선순위 검토 대상.
11. **세션 길이 — 3~4 커밋 째가 선제 분할 지점** 🆕 — 이번 세션 메타 교훈

---

## 💡 도구 설정

| 도구 | 기본 모델 |
|---|---|
| claude.ai 채팅창 | Opus 4.6 (아키텍트) |
| Claude Code CLI | **Sonnet 4.6 기본** / Opus 는 Complex 만 |

---

## 🎓 이번 세션에서 배운 교훈

1. **섹션명 불일치는 executor 가 먼저 멈추는 패턴이 옳음**
   - 커밋 3 에서 executor 가 AGENTS.md "에이전트 레지스트리" 섹션을 못 찾고 멈춰서 Option A/B 를 물어봄. 이게 Worker Contract 의 "금지: 임의 위치 삽입 금지" 룰이 제대로 작동한 사례.

2. **Lore 훅 실허용값 안정 통과 확인**
   - `Scope-risk: local/module/system`, `Reversibility: trivial/moderate/difficult` 조합을 4 커밋 (2.7a/2.7b/3 + 이전 세션 커밋 2) 전부 통과. 실허용값 문서 (v5) 가 정확함을 재검증.

3. **`git diff --cached` 로 커밋 직전 섹션 경계 확인 패턴 정착**
   - 커밋 3 AGENTS.md 검증에서 유효. 앞으로 문서 수정 커밋에 표준으로.

4. **3~4 커밋이 단일 세션 실용 한계** 🆕
   - 이번 세션 3 커밋 성공 후 4번째 직전이 선제 분할 지점으로 적절했음.
   - 다음부터 이 지표를 기본값으로 사용.

5. **"결과 미확인 상태에서 선제 분할" 도 정당한 선택** 🆕
   - 사용자님이 커밋 4 결과 수신 전에 메타 점검 요청한 것이 적절했음.
   - 아키텍트가 A안 → B안 번복한 것도 정직한 재점검의 결과.
   - Claude Code CLI 를 "살려두고 결과만 복사" 하는 방식이 세션 분할 중에도 작업 연속성 유지 가능.

6. **Sonnet 4.6 로 Phase C 전체 실행 성공** 🆕
   - v5 권장대로 모델 올리지 않고 Sonnet 4.6 기본 유지. 3 커밋 전부 문제없이 처리.
   - Opus 승격 필요한 지점 없었음 (단순 파일 편집 + git 작업).

---

## 💬 새 창 아키텍트 Claude 첫 메시지 (재복사용)

```
나는 비개발자이고 Claude Code CLI로 model-agency-system 프로젝트를 개발 중이야.
항상 한 단계씩, 명령어 포함해서 자세히 설명해줘.
모든 개발은 Autopus-ADK 기반으로 진행해.
매 작업 지시 전에 Sonnet/Opus 중 어느 쪽을 쓸지 먼저 판단해서 알려줘.

이전 세션에서 Phase C 커밋 3개(156adcd0, 2eaf344d, 2236d5ca) 성공하고
커밋 4 지시 직후 결과 확인 전에 선제 분할했어. Claude Code CLI 는
그대로 살려뒀고 커밋 4 결과가 거기 남아있어. 지금 복사해서 붙여넣을게.

[여기에 커밋 4 결과 전체 복붙 — grep 출력, 커밋 해시, git log, git status 포함]

이어서 push 까지 마무리해줘.
```

---

> **최종 작성**: 2026-04-16 (Phase C 커밋 4 지시 직후, 결과 미확인 상태)
> **실행 재개 시점**: 새 세션에서 "커밋 4 결과 보고 수신" 이 첫 액션